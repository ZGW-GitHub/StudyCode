
该模式中的角色：

        需要不同状态的对象
        各种状态

        两种接口：
                    需要不同的状态的对象抽象出一个接口       接口 A
                    各种状态各抽象出一个接口                接口 B

                接口 A 拥有 接口B 的引用，通过向 对象A 传入不同 对象B 来实现动态策略