server:
  port: 60000

spring:
  application:
    name: NacosConfig

  cloud:
    nacos:
      config:
        server-addr: linux.juststudy.site:8848
        file-extension: yaml
        group: COMMON_GROUP
